{
  "name": "test-suite",
  "version": "1.0.0",
  "description": "A test suite for visual blocks editor",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.13.0",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "cy:run": "yarn run cypress run",
    "cy:open": "yarn run cypress open",
    "cy:seed": "docker compose -p dissertation-test up --build mongo-seed"
  },
  "dependencies": {
    "@4tw/cypress-drag-drop": "^2.2.4",
    "cypress-real-events": "^1.8.1"
  }
}
